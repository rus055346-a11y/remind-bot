# credentials.json — сервисный ключ Google. Любая правка переводов строк
# ломает JWT-подпись и валит панель. Запрещаем git его конвертировать.
credentials.json -text

# ngrok.exe и другие бинарники тоже не трогать
*.exe -text
